apiVersion: v1
kind: Service
metadata:
  name: recipes-svc
  namespace: recipes
  labels:
    app: recipes
spec:
  type: NodePort
  selector:
    app: recipes
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30081
