apiVersion: v1
kind: Service
metadata:
  name: api-gateway-service
  namespace: boutique-app
spec:
  type: ClusterIP
  selector:
    app: api-gateway-service
  ports:
    - name: http
      port: 4000
      targetPort: 4000
