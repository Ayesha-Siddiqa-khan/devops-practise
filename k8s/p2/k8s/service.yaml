apiVersion: v1
kind: Service
metadata:
  name: flask-calculator-service
spec:
  selector:
    app: flask-calculator
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30050
  type: NodePort
